@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<form asp-action="AddExam2" asp-controller="Admin">
    <div class="form-group">

   <p><select name="id">
        <option disabled>Выберите дисциплину</option>
        @foreach (var item in ViewBag.UPlanUnit)
       {
           <option value="@item.ID">@item.NameGroup @item.NameType @item.NamePredmet @item.NameTeacher</option>
       }
   </select></p>

   <p>
   <label>Выберите дату экзамена</label>
   <input type="date" name="dateEx"/>
   </p>
        <input type="submit" value="Отправить" class="btn btn-default" />
    </div>
</form>